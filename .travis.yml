
langauge: cpp
compiler: gcc
sudo: required
dist: trusty

install:
    - sudo apt-get update
    - sudo apt-get install gfortran libblas-dev liblapack-dev

script:
  - ./configure -b dev -c y
  - make
  - cd ./tests
  - ./cov_setup
  - cd ..
  - rm -rf ./src/misc/logit_transform.*

after_success:
  - bash <(curl -s https://codecov.io/bash)

after_failure:
  - ./travis-tool.sh dump_logs

notifications:
  email:
    on_success: change
    on_failure: change
